from tkinter import *

window = Tk()

window.title("Financial Independence Calculator")

lbl = Label(window, text="Welcome to The Financial Independence Calculator")
lbl.grid(column=0, row=0)


window.mainloop()